import styled from 'styled-components'
import Rectangle from 'react-rectangle'

export default styled(Rectangle)`
  border: 2px dashed #DCDFE6;
  border-radius: 8px;
  box-sizing: border-box;
  font-weight: bold;
  margin: -2px;

  align-items: center;
  justify-content: center;
`
